## 基线同步
- 镜像上游 v1.0.30（origin `0249c73` -> `bfd153c`，2 个 commit：#1054 + #1055）。
- 前端 `ServerForm` 同步上游 3 分区布局重构（Basic Info 列网格、OAuth2/OpenID Connect 配置块顺序、`+` 按钮统一 hub 样式、passthrough headers 与 OAuth 配置移入 Advanced 分区按 serverType 路由）；保留桌面端差异：隐藏 visibility 选择器（所有 server 默认公开）、`getInitialServerType` 跳过 `builtin`、visibility 默认 `public`。
- 前端 `Button`/`EmbeddingSyncContext` 同步上游修复（async `onClick` 自动 loading、`data.progress` 类型收窄闭包持有）。
- 前端 `serverFormPayload`：timeout 始终回显（不因等于默认 60000 而丢弃，避免触发后端「连接变更」误判）；`proxy` 字段 round-trip。
- locales（en/zh/fr/tr）：补齐 `visibility*` + `share*` 共 14 键，`group` 从「预留」改为「共享给指定用户」语义。

## Rust 后端
- **编辑服务器不再无谓重连**（镜像上游 #1055）：`update_server` 比对连接相关字段（command/url/args/env/headers/options/openapi/perSessionClient/startOnDemand/idleTimeoutMs/proxy/keepAlive/type/enabled），无连接相关变更时仅持久化 + 保留实时连接，不再杀掉 MCP 连接 / 重启 stdio 子进程。默认请求超时 60000 与「未设置」视为等价。
- **proxychains 配置持久化**：`ServerConfig` 新增 `proxy` 字段（`ProxychainsConfig`），DB v20 迁移加 `servers.proxy` 列，SELECT/INSERT/UPDATE/`map_row` 全链路持久化，使前端 round-trip 真正生效（此前 serde 默默丢弃该字段）。
- DB 迁移版本 19 -> 20（`migrate_v20` + `0020_server_proxy_column.sql`）。

## 未同步（评估后跳过）
- 上游 #1054 group visibility / sharedWithUsers（按用户过滤可见性、share-candidates 端点）：桌面端 Rust 无 visibility/owner 概念、单用户免登录、前端已隐藏可见性 UI；类型字段与翻译已对齐（dormant），待将来多用户实现时落地。
- 上游 cookieSession（OpenAPI Set-Cookie 跨调用持久化，#1047 体系）：桌面端 `rmcp-openapi` 传输无 cookie 持久化，toggle 为 no-op，前端注释隐藏。
