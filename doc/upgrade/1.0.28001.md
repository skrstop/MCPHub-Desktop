## 新功能
- skill管理
- RAG beta版, 打开后，将会在mcp中提供`rag_search`、`rag_get`、`rag_tag_search`等工具
- stdio 服务器支持「按需启动」（startOnDemand）：勾选后跳过启动连接，首次工具调用时才拉起进程，空闲超时（默认 5 分钟）自动关闭，降低常驻内存占用。进程关闭后仍保留工具列表缓存，下次调用自动冷启动。

## 修复
- stdio 服务器连接失败时，错误信息包含上游进程的 stderr 输出（如 Python traceback、缺失依赖等），便于排查
- 其他已知问题修复
- 升级相关依赖类库版本
